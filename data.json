---
layout: none
---
[
  {% for course in site.courses %}
    {
      "id": "{{ course.slug }}",
      "title": "{{ course.title }}",
      "desc": "{{ course.desc }}",
      "image": "{{ course.image }}"
    }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]